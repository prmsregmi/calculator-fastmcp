[project]
name = "calculator-fastmcp"
version = "0.2.0"
description = "Simple calculator MCP server using dedalus-mcp (was FastMCP)"
requires-python = ">=3.10"
dependencies = [
    "dedalus-mcp>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
